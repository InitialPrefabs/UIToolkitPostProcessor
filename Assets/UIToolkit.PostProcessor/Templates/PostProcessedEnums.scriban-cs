namespace InitialPrefabs.UIToolkit.Constants {
    public static class {{ name }} {
        {{ for pair in pairs -}}
        public const string {{ pair.key }} = "{{ pair.value }}";
        {{~ end }}
    }
}